# Installing Toast Notifications

Run this command in the frontend directory:

```bash
cd C:\Dev\project-tracker\frontend
npm install react-hot-toast
```

This will install the toast notification library. Once done, let me know and I'll integrate it into all the forms!
